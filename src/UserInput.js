import NavBar from './NavBar';

export default function App() {
	return (
	<div>
		<NavBar />
	</div>
	)
}

function Cost() {
	const userData = ['userCost', 'schloarshipCost'];

	click = () => {
		console.log("The cost of your tuiton per year is" + Cost)
	}
return (
	DataTransfer={data} 
)
}
	


function amount(props) {
	let amountNeeded = props.DataTransfer; //data from schools and schlorship information per school
}

	return 
	<div>
		<button click={findAmountNeeded}></button>
	</div>
